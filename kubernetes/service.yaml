apiVersion: v1
kind: Service
metadata:
  name: deepcloak
  namespace: poc-deepcloak
  labels:
    app: deepcloak
    autopoc.io/project: deepcloak
spec:
  type: ClusterIP
  selector:
    app: deepcloak
  ports:
    - port: 8080
      targetPort: 8080
